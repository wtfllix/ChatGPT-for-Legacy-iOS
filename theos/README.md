# Theos Workspace

This directory contains the active jailbreak-oriented build for Legacy-Chatbox.

## App

```sh
LegacyChatApp/
```

## Build

```sh
cd LegacyChatApp
make package FINALPACKAGE=1
```

The generated `.deb` is written to `LegacyChatApp/packages/`.
